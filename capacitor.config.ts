import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'npm run buildnpx cap add androidnpx cap add iosnpx cap sync',
  webDir: 'public'
};

export default config;
